import chalk from 'chalk';

export async function searchCommand(term) {
  console.log(chalk.yellow(`[stub] Would search registry for term '${term}'.`));
  // Future: call remote registry API and pretty-print results
} 